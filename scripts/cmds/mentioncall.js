module.exports = {
	config: {
		name: "mentioncall",
		version: "2.0",
		author: "Omar",
		role: 0,
		shortDescription: "Reply when someone is mentioned",
		longDescription: "Auto reply caption when any user is mentioned",
		category: "group"
	},

	onChat: async function({ event, api }) {
		try {

			// যদি mention না থাকে → exit
			if (!event.mentions || Object.keys(event.mentions).length === 0) return;

			const botID = api.getCurrentUserID();

			for (const uid in event.mentions) {

				// bot ignore
				if (uid == botID) continue;

				const name = event.mentions[uid].replace("@", "");

				// 20 টা captions
				const captions = [
					`${name} 📢 আপনাকে ডাকা হচ্ছে, দ্রুত গ্রুপে হাজির হন!`,
					`${name} 🚨 সবাই আপনাকে খুঁজছে, প্লিজ চলে আসুন!`,
					`${name} 👀 কোথায় আপনি? আপনার অপেক্ষায় আছি!`,
					`${name} 📞 জরুরি ডাক! এখনই আসুন।`,
					`${name} ⏰ সময় হয়েছে হাজির হওয়ার!`,
					`${name} 🔥 গ্রুপ জমে গেছে, শুধু আপনাকেই দরকার!`,
					`${name} 💬 আলোচনা চলছে — আপনাকে ছাড়া অসম্পূর্ণ!`,
					`${name} ⚡ দ্রুত আসুন! বিশেষ ডাক এসেছে!`,
					`${name} 🎯 টার্গেট আপনি 😄 এখনই রিপোর্ট করুন!`,
					`${name} 📣 আপনাকে অবিলম্বে হাজির হতে বলা হচ্ছে!`,
					`${name} 😎 আপনাকে ছাড়া গ্রুপটা ফাঁকা লাগছে!`,
					`${name} 🚀 দ্রুত ল্যান্ড করুন গ্রুপে!`,
					`${name} 🔔 নোটিফিকেশন: আপনার উপস্থিতি জরুরি!`,
					`${name} 🎉 সবাই আছে, শুধু আপনি নাই!`,
					`${name} 🛎️ আপনার জন্য কল এসেছে 😁`,
					`${name} 🌟 VIP ডাক — দয়া করে প্রবেশ করুন!`,
					`${name} 📍 আপনার লোকেশন দরকার 😄`,
					`${name} 🤖 সিস্টেম বলছে — এখনই যোগ দিন!`,
					`${name} 🥳 মজা শুরু হয়েছে, আপনাকে মিস করছি!`,
					`${name} 🧲 সবাই টানছে — এখন শুধু আপনাকেই চাই!`
				];

				// random caption
				const msg = captions[Math.floor(Math.random() * captions.length)];

				// send message
				api.sendMessage(msg, event.threadID, event.messageID);
			}

		} catch (err) {
			console.log("MentionCall Error:", err);
		}
	}
};
